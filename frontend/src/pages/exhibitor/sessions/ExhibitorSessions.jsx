// Exhibitors browse and register for sessions using the same
// interface as attendees — the AttendeeSessions page handles
// both roles via the same API endpoints.
export { default } from '@/pages/attendee/sessions/AttendeeSessions';